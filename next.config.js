/** @type {import('next').NextConfig} */
const nextConfig = {
  // This makes Next generate a static site in the `out/` folder after `npm run build`
  output: 'export',
};

module.exports = nextConfig;
